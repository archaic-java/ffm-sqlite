# ffm-sqlite

JDK 25; Linux and system `libsqlite3.so.0`. Use named modules and `javac`/`java` argument files, never a class path or build tool. The catalog contract, catalog tests, and Minau runner are linked at `lib/src/`. Generated classes belong in ignored `out/`.
