# ffm-sqlite

JDK 25; Linux and system `libsqlite3.so.0`. Use named modules and `javac`/`java` argument files, never a class path or build tool. The catalog contract is linked at `lib/src/work.archaic.service.catalog`. Generated classes belong in ignored `out/`.
